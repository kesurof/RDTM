# Real-Debrid API Token (obligatoire)
RD_API_TOKEN=your_real_debrid_api_token_here

# Chemin vers vos médias (optionnel)
MEDIA_PATH=/medias

# Port du serveur (optionnel)
PORT=8000

# Niveau de log (optionnel)
LOG_LEVEL=INFO
