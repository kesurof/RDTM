# Configuration Real-Debrid Manager
# Copier ce fichier vers .env et compléter les valeurs

# === OBLIGATOIRE ===
# Token API Real-Debrid (récupérer sur https://real-debrid.com/apitoken)
RD_API_TOKEN=your_api_token_here

# === OPTIONNEL ===
# Webhook Discord pour notifications d'échecs définitifs
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Niveau de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Intervalle entre scans en secondes (défaut: 600 = 10 minutes)
SCAN_INTERVAL=600

# Mode dry-run par défaut (true/false)
DRY_RUN=true

# Nombre de workers parallèles (défaut: 4)
MAX_WORKERS=4

# === AVANCÉ ===
# Délai minimum entre requêtes API en secondes
API_RATE_LIMIT=1.0

# Nombre maximum de tentatives de réinjection par torrent
MAX_RETRY_ATTEMPTS=3

# Délai entre tentatives de réinjection en heures
RETRY_DELAY_HOURS=3